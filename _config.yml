baseurl: "/"

permalink: pretty

collections:
  team:
    output: true

# Language settings
languages: [ "nl", "en" ]
default_language: "nl"
exclude_from_localization: [ "assets", "admin", "js", "css", "images", "fonts" ]
parallel_localization: true

# Add to your plugins list if you have one already
plugins:
- jekyll-multiple-languages-plugin
